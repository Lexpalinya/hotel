server {
    listen 80;
    server_name hotel.lifefootball.la;

    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}
