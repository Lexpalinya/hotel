import{createClient}from'@/lib/supabase-server';import{WTopBar}from'@/components/staff-bits';import CustomerManager from'./customer-manager';export const dynamic='force-dynamic';export default async function CustomersPage(){const db=createClient();const{data}=await db.from('customers').select('*').order('created_at',{ascending:false});return <><WTopBar title="ຈັດການຂໍ້ມູນລູກຄ້າ" sub={`${data?.length??0} ລາຍການ`}/><CustomerManager customers={data??[]}/></>}
